import { StyleSheet } from "react-native";
import { PRIMARY_DARK } from "../../../styles/colors";

export const styles = StyleSheet.create({
  content: { display: "flex", marginTop: 25 },
  title: {
    fontFamily: "Gilroy-ExtraBold",
    fontSize: 24,
    textAlign: "center",
    color: PRIMARY_DARK,
  },
});
